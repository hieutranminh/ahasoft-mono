import type { App } from 'vue'

// Import global components here
// Example: import BaseButton from '@/components/Base/BaseButton.vue'

export function setupGlobalComponents(_app: App): void {
  // Register global components that will be used frequently
  // app.component('BaseButton', BaseButton)
  // For now, this is a placeholder - add components as needed
  // console.warn('Global components registered')
}
